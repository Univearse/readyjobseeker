/** @type {import('next').NextConfig} */
const nextConfig = {
  // tomiwa: Configure allowed image domains
  images: {
    domains: ['images.unsplash.com'], // Allow images from Unsplash
  },
};

export default nextConfig;
